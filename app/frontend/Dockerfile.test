# Frontend Test Dockerfile - Uses base image with dependencies pre-installed
FROM absequencealign-frontend-base:latest

# Set working directory
WORKDIR /app

# Copy all source code (excluding node_modules which are already in base image)
COPY . .

EXPOSE 5679

# Default command - run tests
CMD ["sh", "-c", "npm run test && npm run type-check && npm run lint"]
